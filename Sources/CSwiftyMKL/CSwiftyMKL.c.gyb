#include "include/CSwiftyMKL.h"
#include <math.h>

% for t in ('float','double'):
${t} smSum_${t}(const ${t}* restrict pSrc, int len) {
  ${t} r = 0;
  #pragma clang loop interleave(enable)
  for (int i=0; i<len; ++i) { r += pSrc[i]; }
  return r;
}
% end # t

